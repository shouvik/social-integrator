global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'oauth-connector-sdk'
    static_configs:
      - targets: ['app:9090']
        labels:
          environment: 'production'
          service: 'oauth-sdk'

